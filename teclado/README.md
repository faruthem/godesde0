# Teclado

Paquete para aprender entrada de datos por teclado.

## Contenido

### ingresos.go
- Uso de `bufio.NewScanner` para leer entrada
- Lectura de integers (`strconv.Atoi`)
- Lectura de strings
- Manejo de errores con `panic`